# Screenshots

No private Codex UI screenshots are bundled in this repository. Use the
text-based session export in `../session-export.md` plus the validation commands
in `../../README.md` as the portable coding-agent evidence.

If the final submission needs visual proof, capture screenshots from the local
owner machine after opening the Codex session and UiPath Cloud demo.
